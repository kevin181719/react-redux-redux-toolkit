in  store file redux data will store in store.js file so use the useSelector hook to access the data 


any action in any file action will call in UI so use useDispatch hook to call the action

UI components se leke data ko redux me store karvane ke liye data.push and action.payload me se data miljayenga

decimal ke baad 2 digit number ke liye toFixed use 

data fetch from ui to redux dispatch , redux to ui data so useSelector

in rtk get action perform so builder.query use and other all call builder.mutation